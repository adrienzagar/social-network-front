import React from "react";
import PropTypes from "prop-types";

const Home = () : JSX.Element => {
	return (
		<div>
            Hello depuis Home
		</div>
	);
};

Home.propTypes = {

};

export default Home;
